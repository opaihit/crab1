using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedType : MonoBehaviour
{
    // Start is called before the first frame update
    public float ImportantFloat;
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
